replicaCount: 1
service:
  port: 8080

image:
  ref: nginx:latest

configMap:
  annotations:
  labels:

fallback:

native:
  object: {}
  array: []
  boolean: true
  integer: 1
  number: 1.5
  nullable:

packageOverride: false
packageNull: true
valuesFileOverride: false
